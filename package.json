{
  "name": "ryder-pick-em",
  "version": "1.0.0",
  "description": "College football odds board (DraftKings lines via TheRundown API), served through a small proxy that keeps the API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
